import uses from './uses.svg';
import oneRound from './one-round.svg';
import permanent from './permanent.svg';

export const DURATION = {
  USES: 'uses',
  ONE_ROUND: 'one_round',
  PERMANENT: 'permanent',
};

export const DURATION_IMAGES = {
  [DURATION.USES]: uses,
  [DURATION.ONE_ROUND]: oneRound,
  [DURATION.PERMANENT]: permanent,
};
