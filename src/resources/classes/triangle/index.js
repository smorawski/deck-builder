import abilities from './abilities';
import TAGS, { TAGS_IMAGES } from '../../tags';

export default {
  name: TAGS.CLASSES.TRIANGLE,
  icon: TAGS_IMAGES[TAGS.CLASSES.TRIANGLE],
  abilities,
  handSize: 10,
};
