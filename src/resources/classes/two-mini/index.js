import abilities from './abilities';
import TAGS, { TAGS_IMAGES } from '../../tags';

export default {
  name: TAGS.CLASSES.TWO_MINI,
  icon: TAGS_IMAGES[TAGS.CLASSES.TWO_MINI],
  abilities,
  handSize: 10,
};
