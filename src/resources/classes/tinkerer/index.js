import abilities from './abilities';
import TAGS, { TAGS_IMAGES } from '../../tags';

export default {
  name: TAGS.CLASSES.TINKERER,
  icon: TAGS_IMAGES[TAGS.CLASSES.TINKERER],
  abilities,
  handSize: 12,
};
