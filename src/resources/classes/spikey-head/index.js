import abilities from './abilities';
import TAGS, { TAGS_IMAGES } from '../../tags';

export default {
  name: TAGS.CLASSES.SPIKEY_HEAD,
  icon: TAGS_IMAGES[TAGS.CLASSES.SPIKEY_HEAD],
  abilities,
  handSize: 12,
};
