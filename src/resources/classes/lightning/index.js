import abilities from './abilities';
import TAGS, { TAGS_IMAGES } from '../../tags';

export default {
  name: TAGS.CLASSES.LIGHTNING,
  icon: TAGS_IMAGES[TAGS.CLASSES.LIGHTNING],
  abilities,
  handSize: 10,
};
