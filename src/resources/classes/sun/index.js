import abilities from './abilities';
import TAGS, { TAGS_IMAGES } from '../../tags';

export default {
  name: TAGS.CLASSES.SUN,
  icon: TAGS_IMAGES[TAGS.CLASSES.SUN],
  abilities,
  handSize: 11,
};
