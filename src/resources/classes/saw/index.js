import abilities from './abilities';
import TAGS, { TAGS_IMAGES } from '../../tags';

export default {
  name: TAGS.CLASSES.SAW,
  icon: TAGS_IMAGES[TAGS.CLASSES.SAW],
  abilities,
  handSize: 10,
};
