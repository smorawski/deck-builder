import abilities from './abilities';
import TAGS, { TAGS_IMAGES } from '../../tags';

export default {
  name: TAGS.CLASSES.BRUTE,
  icon: TAGS_IMAGES[TAGS.CLASSES.BRUTE],
  abilities,
  handSize: 10,
};
