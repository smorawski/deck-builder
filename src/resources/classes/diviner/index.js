import abilities from './abilities';
import TAGS, { TAGS_IMAGES } from '../../tags';

export default {
  name: TAGS.CLASSES.DIVINER,
  icon: TAGS_IMAGES[TAGS.CLASSES.DIVINER],
  abilities,
  handSize: 9,
};
