export const filterTagsSelector = (state) => state.filter.tags;

export const filterLevelsSelector = (state) => state.filter.levels;

export const filterSearchSelector = (state) => state.filter.search;

export const filterSelector = (state) => state.filter;
